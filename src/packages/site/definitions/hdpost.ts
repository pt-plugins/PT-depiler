import type { ISiteMetadata } from "../types";

export const siteMetadata: ISiteMetadata = {
  version: 1,
  id: "hdpost",
  name: "HDPOST",
  tags: ["电影", "电视剧"],

  type: "private",
  schema: "Unit3D",

  urls: ["https://pt.hdpost.top/"],

  isDead: true,
};
