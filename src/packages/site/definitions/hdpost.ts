import type { ISiteMetadata } from "../types";

export const siteMetadata: ISiteMetadata = {
  version: 1,
  id: "hdpost",
  name: "HDPOST",

  type: "private",
  schema: "Unit3D",

  urls: ["uggcf://cg.uqcbfg.gbc/"],

  isDead: true,
};
