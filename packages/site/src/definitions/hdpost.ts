import type { ISiteMetadata } from "../types";

export const siteMetadata: ISiteMetadata = {
  schema: "Unit3D",
  type: "private",
  name: "HDPOST",
  url: "https://pt.hdpost.top/",
  tags: ["电影", "电视剧"],
};
